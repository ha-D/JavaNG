apply plugin: 'java'

sourceCompatibility = 1.5
version = '1.0'

repositories {
    mavenCentral()
}

uploadArchives {
    repositories {
        flatDir {
            dirs 'dist'
        }
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}